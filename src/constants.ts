export const DATA_SEND_CHANNEL = 'data-relay-sender'
export const DATA_RELAY_CHANNEL = 'data-relay'
export const SERVER_SETTING = {
    protocol: 'http',
    hostname: 'localhost',
    port: 3000,
}
export const SERVER_ADDRESS = `${SERVER_SETTING.protocol}://${SERVER_SETTING.hostname}:${SERVER_SETTING.port}`
